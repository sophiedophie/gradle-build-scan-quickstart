---
resources:
- name: source-code
  type: git
  source:
    uri: https://github.com/gradle/gradle-build-scan-quickstart
    branch: test

jobs:
- name: build
  public: true
  plan:
  - get: source-code
    trigger: true
  - task: build
    file: source-code/build.yml
