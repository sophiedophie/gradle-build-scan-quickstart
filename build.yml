---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: alpine
    tag: 'git'

inputs:
  - name: source-code

outputs:
  - name: build-output

run:
  path: source-code/build.sh