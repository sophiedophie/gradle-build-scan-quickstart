---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8-jdk-alpine'

inputs:
  - name: source-code

outputs:
  - name: build-output

run:
  path: bash
  args: [./source-code/build.sh]